import {PageHeader} from "@/components/admin/layout/PageHeader";import {FilterWorkspace} from "./FilterWorkspace";import {EnterpriseDataTable,type Column} from "./EnterpriseDataTable";
export function QueuePage<T extends{id:string;status:string}>({title,description,crumbs,metrics,rows,columns,actionLabel,href}:{title:string;description:string;crumbs:string[];metrics?:{label:string;value:string}[];rows:T[];columns:Column<T>[];actionLabel:string;href:(row:T)=>string}){return <><PageHeader title={title} description={description} crumbs={crumbs}/>{metrics&&<div className="grid kpis">{metrics.map(x=><div className="card" key={x.label}><span className="muted">{x.label}</span><div className="kpi-value">{x.value}</div><small className="muted">Mock summary metric</small></div>)}</div>}<FilterWorkspace/><EnterpriseDataTable rows={rows} columns={columns} actionLabel={actionLabel} href={href}/></>}
