"use client";
import {usePathname,useRouter,useSearchParams} from "next/navigation";import {useEffect,useState} from "react";
export function FilterWorkspace({statusOptions=["pending","approved","high-risk"]}:{statusOptions?:string[]}){const router=useRouter(),path=usePathname(),params=useSearchParams();const [search,setSearch]=useState(params.get("search")??"");useEffect(()=>{const timer=setTimeout(()=>{const next=new URLSearchParams(params.toString());if(search)next.set("search",search);else next.delete("search");router.replace(`${path}${next.size?`?${next}`:""}`,{scroll:false})},350);return()=>clearTimeout(timer)},[search,path,router,params]);function update(name:string,value:string){const next=new URLSearchParams(params.toString());if(value)next.set(name,value);else next.delete(name);router.replace(`${path}${next.size?`?${next}`:""}`,{scroll:false})}return <div className="workspace" aria-label="Queue filters"><input value={search} onChange={e=>setSearch(e.target.value)} placeholder="Search by reference or name" aria-label="Search"/><select aria-label="Status" value={params.get("status")??""} onChange={e=>update("status",e.target.value)}><option value="">All statuses</option>{statusOptions.map(x=><option key={x}>{x}</option>)}</select><select aria-label="Assignment" value={params.get("assigned")??""} onChange={e=>update("assigned",e.target.value)}><option value="">All assignees</option><option value="me">Assigned to me</option><option value="team">My team</option></select><button className="button" onClick={()=>{setSearch("");router.replace(path)}}>Clear filters</button></div>}
