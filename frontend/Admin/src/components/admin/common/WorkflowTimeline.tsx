export function WorkflowTimeline({items=["Case created from operational queue","Automated validation completed","Assigned for administrator review"]}:{items?:string[]}){return <div className="timeline">{items.map((x,i)=><div className="timeline-item" key={x}><strong>{x}</strong><div className="muted">Mock history item {i+1}; production history must come from the service.</div></div>)}</div>}
