"use client";
import React from "react";
export function AllocationKpiGrid({metrics={}}:{metrics?:Record<string,number|null>}) {
 const cards=[['Allocation Requests','total'],['Fully Allocated','fully_allocated'],['Partially Allocated','partially_allocated'],['Allocation Pending','pending'],['Allocation Failed','failed'],['Active Reservations','active_reservations'],['Reservations Expiring Soon','expiring_reservations'],['Reservation Failures','reservation_failures'],['Stock Shortage Cases','stock_shortage'],['Transfers Required','transfer_required'],['Transfers In Transit','transfers_in_transit'],['Allocation SLA Breaches','sla_breaches']] as const;
 const performance=['Fully Allocated Rate','Avg Allocation Time','Reservation Success Rate','Transfer Fulfilment Rate','Avg Transfer Lead Time','Allocation Accuracy'];
 return <div className="space-y-1.5"><div className="grid grid-cols-2 sm:grid-cols-4 lg:grid-cols-6 xl:grid-cols-12 gap-1.5 text-[10px]">{cards.map(([title,key])=><div key={key} className="bg-white p-2 rounded-xl border border-line shadow-xs h-[64px] flex flex-col justify-between"><span className="text-[9px] font-semibold text-muted">{title}</span><strong className="text-base">{Number(metrics[key]??0).toLocaleString()}</strong></div>)}</div><div className="grid grid-cols-2 lg:grid-cols-3 xl:grid-cols-6 gap-1.5">{performance.map(title=><div key={title} className="bg-white p-2 rounded-xl border border-line h-[40px]"><span className="text-[8.5px] text-muted block">{title}</span><strong>—</strong></div>)}</div></div>;
}
