"use client";
import React,{useState}from"react";import{X}from"lucide-react";
export function ScheduleExportDrawer({isOpen,onClose,onSubmit}:{isOpen:boolean;onClose:()=>void;onSubmit:(payload:{name:string;data_type:string;frequency:string;timezone:string;format:"csv"})=>Promise<void>}){const[name,setName]=useState("");const[frequency,setFrequency]=useState("daily");const[busy,setBusy]=useState(false);const[error,setError]=useState("");if(!isOpen)return null;const submit=async(event:React.FormEvent)=>{event.preventDefault();setBusy(true);setError("");try{await onSubmit({name,data_type:"products",frequency,timezone:Intl.DateTimeFormat().resolvedOptions().timeZone,format:"csv"});onClose();}catch(cause){setError(cause instanceof Error?cause.message:"Unable to schedule export.");}finally{setBusy(false);}};return <div className="fixed inset-0 z-50 flex justify-end bg-black/50"><div className="flex h-full w-full max-w-lg flex-col bg-white"><div className="flex items-center justify-between border-b p-5"><h2 className="font-extrabold">Schedule Export</h2><button onClick={onClose}><X size={18}/></button></div><form onSubmit={submit} className="flex-1 space-y-4 p-5 text-[12px]">{error&&<p className="rounded bg-rose-50 p-3 text-rose-700">{error}</p>}<label className="block font-bold">Schedule Name<input required maxLength={255} value={name} onChange={event=>setName(event.target.value)} className="mt-1 h-9 w-full rounded border px-3"/></label><label className="block font-bold">Dataset<select disabled className="mt-1 h-9 w-full rounded border px-3"><option>Product Masters</option></select></label><label className="block font-bold">Frequency<select value={frequency} onChange={event=>setFrequency(event.target.value)} className="mt-1 h-9 w-full rounded border px-3"><option value="daily">Daily</option><option value="weekly">Weekly</option><option value="monthly">Monthly</option></select></label><label className="block font-bold">Format<select disabled className="mt-1 h-9 w-full rounded border px-3"><option>CSV</option></select></label><p className="text-[10px] text-muted">Generated by Laravel Scheduler and processed by the configured queue worker.</p><div className="flex justify-end gap-2 border-t pt-4"><button type="button" onClick={onClose} className="h-9 rounded border px-4">Cancel</button><button disabled={busy} className="h-9 rounded bg-[#671021] px-5 font-bold text-white disabled:opacity-50">{busy?"Saving…":"Create Schedule"}</button></div></form></div></div>}
