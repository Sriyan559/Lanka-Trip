"use client";import React from"react";import{AlertTriangle,Barcode,Ban,CheckCircle2,Copy,FileText,FileWarning,ImageOff,Layers,QrCode,ShieldCheck,Clock}from"lucide-react";import type{QualityKpiCard}from"@/types/catalogueQuality";const icons=[ShieldCheck,FileWarning,AlertTriangle,Copy,Barcode,QrCode,FileText,ImageOff,Layers,Ban,CheckCircle2,Clock];export function QualityKpiGrid({kpis,onFilterClick}:{kpis:QualityKpiCard[];onFilterClick:(scope:string)=>void}){return <div className="mb-4 grid grid-cols-2 gap-3 md:grid-cols-4 xl:grid-cols-6">{kpis.map((kpi,index)=>{const Icon=icons[index]??FileWarning;return <button key={kpi.id} onClick={()=>onFilterClick(kpi.scope)} disabled={!kpi.available} className="rounded-lg border border-line bg-white p-3 text-left shadow-sm disabled:cursor-not-allowed disabled:bg-slate-50"><div className="mb-2 flex justify-between"><span className="text-[10px] font-semibold text-muted">{kpi.label}</span><Icon size={14} className="text-[#671021]"/></div><div className="font-mono text-lg font-black text-ink">{kpi.value===null?"N/A":kpi.label==="Overall Quality Score"?`${kpi.value} /100`:kpi.value.toLocaleString()}</div><span className="text-[9px] text-muted">{kpi.trend===null?"No comparable history":`${kpi.trend}%`}</span></button>})}</div>}
