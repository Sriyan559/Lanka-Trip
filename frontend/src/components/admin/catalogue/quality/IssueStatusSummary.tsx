"use client";import React from"react";import type{IssueStatusSummaryItem}from"@/types/catalogueQuality";export function IssueStatusSummary({data,onStatusClick}:{data:IssueStatusSummaryItem[];onStatusClick:(value:string)=>void}){return <section className="rounded-lg border border-line bg-white p-4 shadow-sm"><h3 className="mb-4 font-mono text-[11px] font-bold uppercase">Issue Status Summary</h3>{data.length?data.map(item=><button key={item.status} onClick={()=>onStatusClick(item.status)} className="mb-2 block w-full text-left"><span className="flex justify-between text-[9px]"><b>{item.status}</b><span>{item.count}</span></span><span className="mt-1 block h-1.5 rounded bg-slate-100"><span className="block h-full rounded" style={{width:`${item.pct}%`,background:item.color}}/></span></button>):<p className="text-xs text-muted">No issue statuses available.</p>}</section>}
