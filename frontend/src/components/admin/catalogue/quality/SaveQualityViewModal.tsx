"use client";import React,{useState}from"react";import{X}from"lucide-react";export function SaveQualityViewModal({isOpen,onClose,onSave}:{isOpen:boolean;onClose:()=>void;onSave:(name:string)=>Promise<void>}){const[name,setName]=useState("");const[saving,setSaving]=useState(false);if(!isOpen)return null;return <div className="fixed inset-0 z-50 flex items-center justify-center bg-black/50"><div className="w-full max-w-md rounded-lg bg-white shadow-xl"><header className="flex justify-between border-b p-4"><h2 className="font-extrabold">Save Custom Quality View</h2><button onClick={onClose}><X size={17}/></button></header><div className="p-5"><label className="text-[11px] font-bold">View name<input autoFocus value={name} onChange={event=>setName(event.target.value)} className="mt-1 h-9 w-full rounded border px-3"/></label></div><footer className="flex justify-end gap-2 border-t p-4"><button onClick={onClose} className="rounded border px-4 py-2">Cancel</button><button disabled={!name.trim()||saving} onClick={()=>{setSaving(true);void onSave(name).then(onClose).finally(()=>setSaving(false))}} className="rounded bg-[#671021] px-4 py-2 font-bold text-white disabled:opacity-50">Save View</button></footer></div></div>}
