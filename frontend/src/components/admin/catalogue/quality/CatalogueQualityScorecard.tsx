import React from"react";import type{ScorecardMetric}from"@/types/catalogueQuality";export function CatalogueQualityScorecard({metrics}:{metrics:ScorecardMetric[]}){return <section className="mb-4 rounded-lg border border-line bg-white p-4 shadow-sm"><h3 className="mb-3 font-mono text-[11px] font-bold uppercase">Catalogue Quality Scorecard</h3><div className="grid grid-cols-2 gap-3 md:grid-cols-4 xl:grid-cols-8">{metrics.map(metric=><div key={metric.label} className="rounded bg-slate-50 p-2"><div className="flex justify-between text-[9px]"><span>{metric.label}</span><b>{metric.percentage===null?"N/A":`${metric.percentage}%`}</b></div><div className="mt-2 h-1.5 rounded bg-slate-200"><div className="h-full rounded bg-emerald-500" style={{width:`${metric.percentage??0}%`}}/></div></div>)}</div></section>}
