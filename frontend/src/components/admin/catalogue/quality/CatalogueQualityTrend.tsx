"use client";import React from"react";import{CartesianGrid,Line,LineChart,ResponsiveContainer,Tooltip,XAxis,YAxis}from"recharts";import type{CatalogueQualityDashboard}from"@/types/catalogueQuality";export function CatalogueQualityTrend({data,granularity,onGranularityChange}:{data:CatalogueQualityDashboard["trend"];granularity:"daily"|"weekly"|"monthly";onGranularityChange:(value:"daily"|"weekly"|"monthly")=>void}){return <section className="flex min-h-[250px] flex-col rounded-lg border border-line bg-white p-4 shadow-sm"><div className="mb-3 flex justify-between"><h3 className="font-mono text-[11px] font-bold uppercase">Catalogue Quality Trend</h3><div className="flex rounded border bg-slate-50 p-1">{(["daily","weekly","monthly"]as const).map(value=><button key={value} onClick={()=>onGranularityChange(value)} className={`rounded px-2 py-1 text-[9px] font-bold ${granularity===value?"bg-white shadow":"text-muted"}`}>{value[0].toUpperCase()+value.slice(1)}</button>)}</div></div>{data.points.length?<div className="h-[190px]"><ResponsiveContainer><LineChart data={data.points}><CartesianGrid strokeDasharray="3 3" vertical={false}/><XAxis dataKey="date" tick={{fontSize:9}}/><YAxis tick={{fontSize:9}}/><Tooltip/><Line dataKey="openIssues" stroke="#0284c7"/><Line dataKey="resolvedIssues" stroke="#059669"/><Line dataKey="criticalIssues" stroke="#dc2626"/><Line dataKey="slaBreaches" stroke="#8b5cf6"/></LineChart></ResponsiveContainer></div>:<div className="flex flex-1 items-center justify-center text-xs text-muted">No historical quality data available.</div>}</section>}
