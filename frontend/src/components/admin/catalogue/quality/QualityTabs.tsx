"use client";import React from"react";export function QualityTabs({tabs,activeTab,onTabChange}:{tabs:Array<{label:string;scope:string;count:number}>;activeTab:string;onTabChange:(tab:string)=>void}){return <div className="mb-4 flex gap-6 overflow-x-auto rounded-t-lg border-b border-line bg-white px-2">{tabs.map(tab=><button key={tab.scope} onClick={()=>onTabChange(tab.label)} className={`whitespace-nowrap px-1 py-2.5 text-[12px] font-semibold ${activeTab===tab.label?"border-b-2 border-[#671021] text-[#671021]":"text-slate-600"}`}>{tab.label} <span className="rounded bg-slate-100 px-1 text-[9px]">{tab.count}</span></button>)}</div>}
