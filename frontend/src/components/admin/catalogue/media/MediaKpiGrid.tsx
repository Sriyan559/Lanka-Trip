"use client";
import React from "react";
import { FolderCheck,CheckCircle2,ShieldCheck,Clock,AlertTriangle,ImageDown,Copy,Link2Off,EyeOff,CalendarX,Globe2,Archive } from "lucide-react";
import type { MediaKpi } from "@/types/mediaManagement";
const icons:Record<string,React.ElementType>={total:FolderCheck,active:CheckCircle2,approved:ShieldCheck,pending:Clock,mandatory:AlertTriangle,lowres:ImageDown,duplicates:Copy,unlinked:Link2Off,alttext:EyeOff,rights:CalendarX,channels:Globe2,archived:Archive};
export function MediaKpiGrid({kpis,onFilterClick}:{kpis:MediaKpi[];onFilterClick:(type:string,value:string)=>void}){return <div className="mb-5 grid grid-cols-2 gap-3 sm:grid-cols-3 md:grid-cols-4 lg:grid-cols-6">{kpis.map(kpi=>{const Icon=icons[kpi.id]||FolderCheck;return <button key={kpi.id} onClick={()=>onFilterClick("scope",kpi.scope)} disabled={!kpi.available} title={kpi.reason||undefined} className="rounded-lg border border-line bg-white p-3 text-left shadow-sm transition hover:shadow-md disabled:cursor-not-allowed disabled:opacity-70"><div className="mb-2 flex items-start justify-between gap-2"><span className="flex h-7 w-7 items-center justify-center rounded border bg-slate-50"><Icon size={14}/></span><span className="text-right text-[11px] font-medium text-slate-500">{kpi.label}</span></div><div className="flex items-baseline justify-between"><b className="text-xl text-ink">{kpi.value==null?"N/A":kpi.value.toLocaleString()}</b><span className="text-[10px] font-bold text-emerald-600">{kpi.trend==null?(kpi.available?"Live":"Unavailable"):`${kpi.trend}%`}</span></div></button>})}</div>}
